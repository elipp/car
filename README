calc:

yet another calculator application. Written in C.
Supports parsing of general mathematical expressions.
User-defined constants (variables).

dependencies: the GNU readline library, 
	      C99 for strtold (only with -DLONG_DOUBLE_PRECISION)
